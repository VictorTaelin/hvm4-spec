@ctru = λt.λf.t
@cfal = λt.λf.f
@cnot = λx.x(@cfal,@ctru)
@P4   = λf.
  ! F &A = f;
  ! F &A = λk. F₀(F₁(k)); // 2^1
  ! F &A = λk. F₀(F₁(k)); // 2^2
  ! F &A = λk. F₀(F₁(k)); // 2^3
  λk. F₀(F₁(k))
@main = @P4(@cnot,@ctru)
